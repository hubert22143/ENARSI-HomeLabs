# Initial Snapshot

This folder is intentionally empty for now.

The lab was exported after the redistribution and route-tagging work was already finished, so there is no honest pre-tagging baseline to store here. I still kept empty `R1.cfg` through `R9.cfg` files so the structure matches the other labs and leaves a clean place for a future starting snapshot.

The finished configs live in `../final/`.

